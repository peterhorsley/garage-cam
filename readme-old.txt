Install vlc using Synaptic Package Manager (install that from the Ubuntu
Software Store) and select vlc and vlc-bin packages (and their dependencies).
Be sure to uninstall VLC from Ubuntu Software Store first.
This is to avoid the issue where cvlc just prints version and terminates.

