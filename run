# to create a python virtual env:
# sudo apt install python3.12-venv
# python3 -m venv .venv
# .venv/bin/pip install -r requirements.txt

cd ~/code/garage-cam
source .venv/bin/activate
python3 garage-cam.py
